import { dashboardMockData } from '../data/dashboardMockData';

// Swap this return value for fetchDashboardFromApi(params) when the endpoint is ready.
export const getDashboardData = () => dashboardMockData;

{
  "name": "main-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix Frontend/billing-react run dev",
    "build": "npm --prefix Frontend/billing-react run build",
    "preview": "npm --prefix Frontend/billing-react run preview",
    "install:all": "npm --prefix Frontend/billing-react install"
  }
}
